import Login from '@/components/Auth/Login';
import Layout from '@/components/Layout';

const LoginPage = () => {
	return (
		<Layout blog>
			<Login />
		</Layout>
	);
};

export default LoginPage;
